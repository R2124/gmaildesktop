{
  "_comment": "OPTIONAL. You normally paste these into the app's Setup screen instead of editing files. If you prefer, copy this to config/credentials.json (git-ignored) and load it manually. Create the values via a Google Cloud 'Desktop app' OAuth client.",
  "clientId": "YOUR_CLIENT_ID.apps.googleusercontent.com",
  "clientSecret": "GOCSPX-your-client-secret"
}
